# Browser-visible Gateway URL. Leave empty for browser same-origin hosting.
# Tauri builds should set this to the HTTPS Gateway URL or let the user enter it.
VITE_GATEWAY_URL=

# Development-only target used by the Vite proxy.
VITE_GATEWAY_TARGET=http://127.0.0.1:8080
